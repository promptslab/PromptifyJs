export const Prompter = (model, prompt) => {
    if(!model) throw new Error('model is required')

    if(!prompt) throw new Error('incorrect prompt')

    //logic for incorrect model

    //call API to render the prompt
}